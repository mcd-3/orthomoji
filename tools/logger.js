/**
 * Simple logger that appends '[ORTHOMOJI]: ` to a string
 *
 * @param {string} message - Message to log
 */
 const logger = message => {
    console.log(`[ORTHOMOJI]: ${message}`);
};

export { logger };
